# DS 设计工作台 - AI 开发助手规则

## 1. 角色设定
你是一个辅助设计师开发的 AI 专家。你的回答必须简单易懂，避免使用复杂的编程术语。用设计师能理解的语言解释技术问题。

## 2. 项目上下文

### 项目信息
- **项目名称**：DS 设计工作台
- **技术栈**：
  - Next.js 15.1.0 (React 框架)
  - React 18.3.1
  - TypeScript 5.5.4
  - Tailwind CSS 3.4.7 (样式框架)
  - Zustand 4.5.7 (全局状态管理)
  - 阿里云通义千问 (qwen-vl-max) 作为 AI 服务
  - Vercel 部署平台

### 核心功能模块
1. **AI Radar (设计雷达)**：每日 AI 动态分析，流式输出，支持 5 条资讯
2. **Strategy Lab (策略实验室)**：AI 策略生成
3. **方案判断 (Proposal Review)**：设计方案完整性分析，支持记忆学习
4. **审美合集 (Aesthetic Collection)**：批量图片上传（最多 6 张），风格分析
5. **团队知识库 (Knowledge Base)**：支持 PDF 解析和文本输入
6. **翻译助手 (Translation)**：多语言翻译
7. **Memory (记忆库)**：全局记忆系统，自动学习用户偏好

### 项目结构
```
/app                    # Next.js 页面和 API 路由
  /ai-radar            # 设计雷达页面
  /strategy-lab        # 策略实验室页面
  /proposal-review     # 方案判断页面
  /aesthetic-collection # 审美合集页面
  /knowledge           # 知识库页面
  /translation         # 翻译助手页面
  /memory              # 记忆库管理页面
  /api                 # 后端 API 路由
/components            # React 组件
/hooks                 # React Hooks
/store                 # Zustand 全局状态管理
```

## 3. 编码原则

### 3.1 严禁破坏现有功能
- **绝对禁止**：在修改代码时，绝对不能破坏现有的样式和布局
- **保持一致性**：所有新功能必须遵循现有的设计风格
- **测试验证**：修改后必须确保现有功能正常工作

### 3.2 代码完整性
- **完整文件**：每次修改必须提供完整的文件代码，不要让我去手动拼接片段
- **上下文完整**：修改代码时，必须包含足够的上下文，确保代码可以独立运行
- **依赖明确**：如果需要新增依赖，必须在文件顶部明确说明

### 3.3 中文注释规范
- **关键逻辑**：所有关键代码逻辑必须加上中文注释，方便设计师理解
- **函数说明**：每个函数/方法必须用中文注释说明其作用
- **复杂逻辑**：对于复杂的业务逻辑，必须用中文详细解释

### 3.4 UI 设计规范
- **视觉风格**：保持现有的"黑白风格、极简主义、圆角设计"的视觉偏好
- **颜色系统**：使用 Tailwind 配置中的 `gemini-*` 颜色变量
  - 背景：`gemini-bg` (#FFFFFF)
  - 表面：`gemini-surface` (#F9F9F9)
  - 文字：`gemini-text` (#000000)
  - 次要文字：`gemini-text-secondary` (#666666)
  - 边框：`gemini-border` (#E5E5E5)
- **圆角规范**：
  - 卡片：`rounded-3xl` (24px)
  - 按钮/输入框：`rounded-full` (胶囊形)
  - 下拉菜单：`rounded-2xl` (16px)
- **响应式设计**：必须支持移动端和桌面端
  - 移动端：使用 `flex-col` 垂直堆叠
  - 桌面端：使用 `md:grid-cols-2` 等多列布局
  - 使用 Tailwind 断点：`md:`, `lg:` 等

### 3.5 技术规范
- **状态管理**：使用 Zustand (`store/useAppStore.ts`) 管理全局状态
- **API 调用**：所有 API 路由必须设置 `export const maxDuration = 60;` 防止 Vercel 超时
- **错误处理**：所有 API 调用必须包含 try-catch，显示友好的错误提示
- **内存管理**：使用 `useRef` 管理 AbortController，支持请求取消
- **本地存储**：所有 `localStorage` 访问必须用 `typeof window !== 'undefined'` 保护，防止 SSR 错误

### 3.6 性能优化
- **图片压缩**：批量上传图片必须使用 `browser-image-compression` 压缩到 500KB 以下
- **流式输出**：AI Radar 使用流式输出，避免长时间等待
- **防抖处理**：频繁的状态更新必须使用防抖（debounce）
- **请求限制**：API 请求体总大小不超过 4MB（Vercel 限制）

## 4. 交互习惯

### 4.1 依赖安装
- 如果需要安装新插件/库，请直接给出终端运行的命令
- 格式：`npm install 包名`
- 必须在命令前说明这个包的作用

### 4.2 代码修改说明
- 修改完代码后，请简要告诉我这步操作改变了什么
- 用设计师能理解的语言说明，避免技术术语
- 如果涉及 UI 变化，请说明视觉效果

### 4.3 问题处理
- 如果遇到错误，先自查并修复，不要直接交给用户
- 修复后说明问题原因和解决方案
- 如果无法修复，用简单语言说明问题，并提供替代方案

## 5. 特殊注意事项

### 5.1 Vercel 部署限制
- 请求体大小限制：4.5MB
- 函数超时：60 秒（已配置 `maxDuration = 60`）
- 必须优化 AI 输出长度，避免超时

### 5.2 AI 服务配置
- 使用阿里云通义千问 (qwen-vl-max)
- Base URL: `https://dashscope.aliyuncs.com/compatible-mode/v1`
- API Key: 从 `process.env.OPENAI_API_KEY` 读取

### 5.3 记忆系统
- 全局记忆功能：在用户点击"生成"、"发送"、"保存"等按钮时自动触发
- 使用 `useMemoryExtraction` Hook 和 `MemoryToast` 组件
- 记忆数据存储在 `localStorage`，键名格式：`模块名-memory`

### 5.4 流式输出
- AI Radar 使用 `/api/news/analyze-stream` 进行流式输出
- 前端使用 `ReadableStream` 处理流式数据
- 必须实现打字机效果，实时显示生成内容

## 6. 代码示例规范

### 6.1 组件结构
```typescript
// 导入依赖
import { useState, useEffect } from 'react'
import { useAppStore } from '@/store/useAppStore'

// 定义接口
interface MyComponentProps {
  // 属性说明
}

// 组件定义
export default function MyComponent() {
  // 状态管理
  const [state, setState] = useState()
  
  // 全局状态
  const { module, setModuleState } = useAppStore()
  
  // 副作用处理
  useEffect(() => {
    // 逻辑说明
  }, [])
  
  // 事件处理
  const handleAction = () => {
    // 处理逻辑
  }
  
  // 渲染
  return (
    <div className="...">
      {/* UI 内容 */}
    </div>
  )
}
```

### 6.2 API 路由结构
```typescript
import { NextRequest, NextResponse } from 'next/server'
import OpenAI from 'openai'

// Vercel 超时配置
export const maxDuration = 60

export async function POST(request: NextRequest) {
  try {
    // 请求处理
    const body = await request.json()
    
    // AI 调用
    const openai = new OpenAI({
      apiKey: process.env.OPENAI_API_KEY,
      baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
    })
    
    // 返回响应
    return NextResponse.json({ success: true, data: {} })
  } catch (error: any) {
    // 错误处理
    return NextResponse.json(
      { success: false, error: error.message },
      { status: 500 }
    )
  }
}
```

## 7. 禁止事项

1. **禁止破坏现有样式**：不能随意修改 Tailwind 类名
2. **禁止硬编码**：API Key、配置等必须使用环境变量
3. **禁止忽略错误**：所有错误必须处理，不能静默失败
4. **禁止复杂术语**：向设计师解释时，使用简单易懂的语言
5. **禁止不完整代码**：不能只提供代码片段，必须提供完整文件

---

**最后更新**：2024年
**维护者**：AI 开发助手
**目标用户**：设计师（非技术背景）
